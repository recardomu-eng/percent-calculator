import streamlit as st

st.title("Калькулятор процентов 🧮")
operation = st.selectbox(
    "Выберите операцию:",
    ["Найти % от числа", "Прибавить % к числу", "Вычесть % из числа"]
)
number = st.number_input("Введите число", value=0.0)
percent = st.number_input("Введите процент", value=0.0)

if st.button("Рассчитать"):
   if operation == "Найти % от числа":
        result = (number * percent) / 100
        st.success(f"Результат: {result}")
   elif operation == "Прибавить % к числу":
        result = number + (number * percent) / 100
        st.success(f"Итоговая сумма: {result}")
   elif operation == "Вычесть % из числа":
        result = number - (number * percent) / 100
        st.success(f"Цена со скидкой: {result}")
        
st.sidebar.header("О проекте ℹ️")
st.sidebar.write("Этот калькулятор создал Durik66.")
import streamlit as st

# 1. Создаем две вкладки
tab1, tab2 = st.tabs(["Процентный калькулятор 🧮", "For Trader 📊"])

# 2. Весь старый код помещаем во вкладку tab1
with tab1:
    st.title("Калькулятор процентов 🧮")
    
    operation = st.selectbox(
        "Выберите операцию:",
        ["Найти % от числа", "Прибавить % к числу", "Вычесть % из числа"]
    )
    
    # Обратите внимание: у всех строк ниже теперь ЕЩЕ БОЛЬШЕ пробелов слева
    number = st.number_input("Введите число", value=0.0, key="num1")
    percent = st.number_input("Введите процент", value=0.0, key="perc1")
    
    if st.button("Рассчитать"):
        # Код внутри кнопки тоже сдвигается еще дальше вправо
        if operation == "Найти % от числа":
            result = (number * percent) / 100
            st.success(f"Результат: {result}")
        # ... и так далее для всех elif ...




